{
  "mcpServers": {
    "redpill": {
      "type": "local",
      "command": "npx",
      "args": ["-y", "redpill-mcp"],
      "tools": ["*"],
      "env": {
        "REDPILL_ALLOW_DECLINE": "0",
        "REDPILL_OBSERVE_ONLY": "0",
        "REDPILL_PERSONA_BACKEND": "stub"
      }
    }
  }
}